function eval() {
    // Do not use eval!!!
    return;
}

function expressionCalculator(expr) {
    let arr = expr.split('');
    for (let el of arr) {
        console.log(el);
        if (el === '+') {

        }
        if (el === '-') {

        }
    }
}


module.exports = {
    expressionCalculator
}
